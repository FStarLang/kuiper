/* Generated by F* Custard extraction. Do not edit. */
#include "Kuiper_Example_Fused_GEMM.h"

__global__ static void kuiper_kernel_0(size_t m, size_t n, size_t k,
                                       custard_f16 *gA, custard_f16 *gB,
                                       custard_f16 *gC);

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(size_t m, size_t n, size_t k,
                                       custard_f16 *gA, custard_f16 *gB,
                                       custard_f16 *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        float sum = 0.0f;
        while (k1 < k) {
            size_t vk = k1;
            float __anf2 = sum;
            size_t ni = ((trow * k) + vk);
            custard_f16 __anf1 = gA[ni];
            size_t ni1 = ((vk * n) + tcol);
            custard_f16 __anf0 = gB[ni1];
            sum = (__anf2 + (sqrtf(kpr_cast_f16_to_f32(__anf1)) *
                             kpr_cast_f16_to_f32(__anf0)));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((trow * n) + tcol);
        (void) (gC[ni_1]);
        custard_f16 v1 = kpr_cast_f32_to_f16(sum);
        size_t ni1_1 = ((trow * n) + tcol);
        gC[ni1_1] = v1;
    }
}

/* C <- sqrt(A) @ B, with A, B, C in fp16 and the accumulation done in fp32. */
void Kuiper_Example_Fused_GEMM_gemm_sqrt_fused(size_t m, size_t n, size_t k,
                                               custard_f16 *gA, custard_f16 *gB,
                                               custard_f16 *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_0, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}
