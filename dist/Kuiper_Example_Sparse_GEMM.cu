/* Generated by F* Custard extraction. Do not edit. */
#include "Kuiper_Example_Sparse_GEMM.h"

__global__ static void kuiper_kernel_0(uint32_t rows, uint32_t cols,
    Kuiper_Sparse_Matrix_smatrix__uint32 gA, uint32_t *gB, uint32_t *gC);

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(uint32_t rows, uint32_t cols,
    Kuiper_Sparse_Matrix_smatrix__uint32 gA, uint32_t *gB, uint32_t *gC)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (rows * cols)) {
        uint32_t trow = (gid / cols);
        uint32_t tcol = (gid % cols);
        uint32_t ri = gA.row_off[trow];
        uint32_t re = gA.row_off[(trow + 1)];
        uint32_t dp = 0;
        uint32_t k = ri;
        while (k < re) {
            uint32_t __anf0 = k;
            uint32_t x = gA.elems[__anf0];
            uint32_t __anf01 = k;
            uint32_t c = gA.col_ind[__anf01];
            uint32_t ni = ((c * cols) + tcol);
            uint32_t y = gB[ni];
            uint32_t __anf02 = dp;
            dp = (__anf02 + (x * y));
            uint32_t __anf03 = k;
            k = (__anf03 + 1);
        }
        uint32_t ni_1 = ((trow * cols) + tcol);
        (void) (gC[ni_1]);
        uint32_t ni1 = ((trow * cols) + tcol);
        gC[ni1] = dp;
    }
}

void Kuiper_Example_Sparse_GEMM__gemm_u32_rr(uint32_t rows, uint32_t shared,
    uint32_t cols, Kuiper_Sparse_Matrix_smatrix__uint32 gA, uint32_t *gB,
    uint32_t *gC)
{
    (void) shared;
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((rows * cols) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_0, (((rows * cols) / 1024) + _ct1),
        ((uint32_t) 1024U), ((uint32_t) 0U), s, rows, cols, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}
