/* Generated by F* Custard extraction. Do not edit. */
#include "Kuiper_GraphDist.h"

__global__ static void kuiper_kernel_0(size_t size, uint16_t *a, uint16_t *b);

__device__ uint16_t Kuiper_GraphDist_add_(uint16_t x, uint16_t y)
{
    if ((x == 0) || ((!(y == 0)) && (y < x)))
        return y;
    else
        return x;
}

__device__ uint16_t Kuiper_GraphDist_mult(uint16_t x, uint16_t y)
{
    if ((x == 0) || (y == 0))
        return 0;
    else
        return ((uint16_t) (x + y));
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(size_t size, uint16_t *a, uint16_t *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (size * size))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / size);
        size_t tcol = (rest % size);
        size_t k = 0;
        uint16_t sum = 0;
        while (k < size) {
            size_t vk = k;
            uint16_t __anf2 = sum;
            size_t ni = ((trow * size) + vk);
            uint16_t __anf1 = a[ni];
            size_t ni1 = ((vk * size) + tcol);
            uint16_t __anf0 = a[ni1];
            sum = Kuiper_GraphDist_add_(__anf2,
                                        Kuiper_GraphDist_mult(__anf1, __anf0));
            size_t __anf01 = k;
            k = (__anf01 + 1);
        }
        size_t ni_1 = ((trow * size) + tcol);
        uint16_t v0 = b[ni_1];
        uint16_t v1 = Kuiper_GraphDist_add_(v0, sum);
        size_t ni1_1 = ((trow * size) + tcol);
        b[ni1_1] = v1;
    }
}

void Kuiper_GraphDist_matmul_dist_gpu(size_t size, uint16_t *a, uint16_t *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (size * size)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_0, (((1 * (size * size)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, size, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}
