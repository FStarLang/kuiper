/* Generated by F* Custard extraction. Do not edit. */
#include "Kuiper_Example_Float32GPU.h"

__global__ __launch_bounds__(1) static void kuiper_kernel_0(
    float x, float y, float *device);
__global__ __launch_bounds__(1) static void kuiper_kernel_1(
    float x, float *device);
__global__ __launch_bounds__(1) static void kuiper_kernel_2(
    float x, float *device);
__global__ __launch_bounds__(1) static void kuiper_kernel_3(
    float x, float *device);

/* hoisted by the Custard Kuiper rule */
__global__ __launch_bounds__(1) static void kuiper_kernel_0(
    float x, float y, float *device)
{
    float result = kpr_f32_mul_rn_ftz(x, y);
    device[0] = result;
}

/* hoisted by the Custard Kuiper rule */
__global__ __launch_bounds__(1) static void kuiper_kernel_1(
    float x, float *device)
{
    float result = kpr_f32_exp2_approx_ftz(x);
    device[0] = result;
}

/* hoisted by the Custard Kuiper rule */
__global__ __launch_bounds__(1) static void kuiper_kernel_2(
    float x, float *device)
{
    float result = kpr_f32_rcp_approx_ftz(x);
    device[0] = result;
}

/* hoisted by the Custard Kuiper rule */
__global__ __launch_bounds__(1) static void kuiper_kernel_3(
    float x, float *device)
{
    float result = kpr_f32_rsqrt_approx_ftz(x);
    device[0] = result;
}

float Kuiper_Example_Float32GPU_multiply(float x, float y)
{
    float out = 0.0f;
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((uint32_t) 4U), ((uint32_t) 1U));
    float *device = (float *) tmp;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_KCALL(kuiper_kernel_0, ((uint32_t) 1U), ((uint32_t) 1U),
        ((uint32_t) 0U), s, x, y, device);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    KPR_MEMCPY_D2H(&out, device, ((uint32_t) 4U));
    KPR_GPU_FREE(device);
    return out;
}

float Kuiper_Example_Float32GPU_exponentiate(float x)
{
    float out = 0.0f;
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((uint32_t) 4U), ((uint32_t) 1U));
    float *device = (float *) tmp;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_KCALL(kuiper_kernel_1, ((uint32_t) 1U), ((uint32_t) 1U),
        ((uint32_t) 0U), s, x, device);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    KPR_MEMCPY_D2H(&out, device, ((uint32_t) 4U));
    KPR_GPU_FREE(device);
    return out;
}

float Kuiper_Example_Float32GPU_reciprocal(float x)
{
    float out = 0.0f;
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((uint32_t) 4U), ((uint32_t) 1U));
    float *device = (float *) tmp;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_KCALL(kuiper_kernel_2, ((uint32_t) 1U), ((uint32_t) 1U),
        ((uint32_t) 0U), s, x, device);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    KPR_MEMCPY_D2H(&out, device, ((uint32_t) 4U));
    KPR_GPU_FREE(device);
    return out;
}

float Kuiper_Example_Float32GPU_inverse_root(float x)
{
    float out = 0.0f;
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((uint32_t) 4U), ((uint32_t) 1U));
    float *device = (float *) tmp;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_KCALL(kuiper_kernel_3, ((uint32_t) 1U), ((uint32_t) 1U),
        ((uint32_t) 0U), s, x, device);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    KPR_MEMCPY_D2H(&out, device, ((uint32_t) 4U));
    KPR_GPU_FREE(device);
    return out;
}
