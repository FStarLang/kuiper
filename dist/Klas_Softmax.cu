/* Generated by F* Custard extraction. Do not edit. */
#include "Klas_Softmax.h"

__global__ static void kuiper_kernel_0(custard_f16 *a_, size_t nthm,
                                       size_t lena, custard_f16 *maxs);
__global__ static void kuiper_kernel_1(size_t lena, custard_f16 *maxs,
                                       custard_f16 *a_);
__global__ static void kuiper_kernel_2(size_t lena, custard_f16 *a_, size_t nth,
                                       custard_f16 *sums);
__global__ static void kuiper_kernel_3(size_t lena, custard_f16 *sums,
                                       custard_f16 *a_);
__global__ static void kuiper_kernel_4(float *a_, size_t nthm, size_t lena,
                                       float *maxs);
__global__ static void kuiper_kernel_5(size_t lena, float *maxs, float *a_);
__global__ static void kuiper_kernel_6(size_t lena, float *a_, size_t nth,
                                       float *sums);
__global__ static void kuiper_kernel_7(size_t lena, float *sums, float *a_);
__global__ static void kuiper_kernel_8(double *a_, size_t nthm, size_t lena,
                                       double *maxs);
__global__ static void kuiper_kernel_9(size_t lena, double *maxs, double *a_);
__global__ static void kuiper_kernel_10(size_t lena, double *a_, size_t nth,
                                        double *sums);
__global__ static void kuiper_kernel_11(size_t lena, double *sums, double *a_);
__global__ static void kuiper_kernel_12(custard_f16 *a_, size_t nthm,
                                        size_t lena, custard_f16 *maxs);
__global__ static void kuiper_kernel_13(size_t lena, custard_f16 *maxs,
                                        custard_f16 *a_);
__global__ static void kuiper_kernel_14(size_t lena, custard_f16 *a_,
                                        custard_f16 *sums);
__global__ static void kuiper_kernel_15(size_t lena, custard_f16 *sums,
                                        custard_f16 *a_);
__global__ static void kuiper_kernel_16(float *a_, size_t nthm, size_t lena,
                                        float *maxs);
__global__ static void kuiper_kernel_17(size_t lena, float *maxs, float *a_);
__global__ static void kuiper_kernel_18(size_t lena, float *a_, float *sums);
__global__ static void kuiper_kernel_19(size_t lena, float *sums, float *a_);
__global__ static void kuiper_kernel_20(double *a_, size_t nthm, size_t lena,
                                        double *maxs);
__global__ static void kuiper_kernel_21(size_t lena, double *maxs, double *a_);
__global__ static void kuiper_kernel_22(size_t lena, double *a_, double *sums);
__global__ static void kuiper_kernel_23(size_t lena, double *sums, double *a_);
__global__ static void kuiper_kernel_24(custard_f16 *a_, size_t nthm,
                                        size_t lena, custard_f16 *maxs);
__global__ static void kuiper_kernel_25(size_t lena, custard_f16 *maxs,
                                        custard_f16 *a_);
__global__ static void kuiper_kernel_26(size_t lena, custard_f16 *a_,
                                        size_t nth, custard_f16 *sums);
__global__ static void kuiper_kernel_27(size_t lena, custard_f16 *sums,
                                        custard_f16 *a_);
__global__ static void kuiper_kernel_28(float *a_, size_t nthm, size_t lena,
                                        float *maxs);
__global__ static void kuiper_kernel_29(size_t lena, float *maxs, float *a_);
__global__ static void kuiper_kernel_30(size_t lena, float *a_, size_t nth,
                                        float *sums);
__global__ static void kuiper_kernel_31(size_t lena, float *sums, float *a_);
__global__ static void kuiper_kernel_32(double *a_, size_t nthm, size_t lena,
                                        double *maxs);
__global__ static void kuiper_kernel_33(size_t lena, double *maxs, double *a_);
__global__ static void kuiper_kernel_34(size_t lena, double *a_, size_t nth,
                                        double *sums);
__global__ static void kuiper_kernel_35(size_t lena, double *sums, double *a_);
__global__ static void kuiper_kernel_36(custard_f16 *a_, size_t nthm,
                                        size_t lena, custard_f16 *maxs);
__global__ static void kuiper_kernel_37(size_t lena, custard_f16 *maxs,
                                        custard_f16 *a_);
__global__ static void kuiper_kernel_38(size_t lena, custard_f16 *a_,
                                        custard_f16 *sums);
__global__ static void kuiper_kernel_39(size_t lena, custard_f16 *sums,
                                        custard_f16 *a_);
__global__ static void kuiper_kernel_40(float *a_, size_t nthm, size_t lena,
                                        float *maxs);
__global__ static void kuiper_kernel_41(size_t lena, float *maxs, float *a_);
__global__ static void kuiper_kernel_42(size_t lena, float *a_, float *sums);
__global__ static void kuiper_kernel_43(size_t lena, float *sums, float *a_);
__global__ static void kuiper_kernel_44(double *a_, size_t nthm, size_t lena,
                                        double *maxs);
__global__ static void kuiper_kernel_45(size_t lena, double *maxs, double *a_);
__global__ static void kuiper_kernel_46(size_t lena, double *a_, double *sums);
__global__ static void kuiper_kernel_47(size_t lena, double *sums, double *a_);

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(custard_f16 *a_, size_t nthm,
                                       size_t lena, custard_f16 *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 v0 = a_[tid];
    custard_f16 acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        custard_f16 __anf0 = acc;
        acc = kpr_hfmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = kpr_hfmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        custard_f16 res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_1(size_t lena, custard_f16 *maxs,
                                       custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = maxs[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_sub(vb, va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_2(size_t lena, custard_f16 *a_, size_t nth,
                                       custard_f16 *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 acc = CUSTARD_F16_LIT(0U);
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        custard_f16 v_ = hexp(v);
        (void) (acc);
        custard_f16 __anf0 = acc;
        acc = custard_f16_add(__anf0, v_);
        size_t __anf01 = idx;
        idx = (__anf01 + nth);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nth) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nth) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = custard_f16_add(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        custard_f16 res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_3(size_t lena, custard_f16 *sums,
                                       custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = sums[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_div(hexp(vb), va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_4(float *a_, size_t nthm, size_t lena,
                                       float *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float v0 = a_[tid];
    float acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        float __anf0 = acc;
        acc = fmaxf(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = fmaxf(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        float res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_5(size_t lena, float *maxs, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = maxs[row];
        float vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_6(size_t lena, float *a_, size_t nth,
                                       float *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float acc = 0.0f;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        float v_ = expf(v);
        (void) (acc);
        float __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + nth);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nth) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nth) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        float res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_7(size_t lena, float *sums, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = sums[row];
        float vb = a_[col];
        a_[col] = (expf(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_8(double *a_, size_t nthm, size_t lena,
                                       double *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double v0 = a_[tid];
    double acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        double __anf0 = acc;
        acc = fmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = fmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        double res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_9(size_t lena, double *maxs, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = maxs[row];
        double vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_10(size_t lena, double *a_, size_t nth,
                                        double *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double acc = 0.0;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        double v_ = exp(v);
        (void) (acc);
        double __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + nth);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nth) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nth) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        double res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_11(size_t lena, double *sums, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = sums[row];
        double vb = a_[col];
        a_[col] = (exp(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_12(custard_f16 *a_, size_t nthm,
                                        size_t lena, custard_f16 *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 v0 = a_[tid];
    custard_f16 acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        custard_f16 __anf0 = acc;
        acc = kpr_hfmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = kpr_hfmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        custard_f16 res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_13(size_t lena, custard_f16 *maxs,
                                        custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = maxs[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_sub(vb, va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_14(size_t lena, custard_f16 *a_,
                                        custard_f16 *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 acc = CUSTARD_F16_LIT(0U);
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        custard_f16 v_ = hexp(v);
        (void) (acc);
        custard_f16 __anf0 = acc;
        acc = custard_f16_add(__anf0, v_);
        size_t __anf01 = idx;
        idx = (__anf01 + 1024);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < 1024) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < 1024) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = custard_f16_add(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        custard_f16 res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_15(size_t lena, custard_f16 *sums,
                                        custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = sums[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_div(hexp(vb), va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_16(float *a_, size_t nthm, size_t lena,
                                        float *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float v0 = a_[tid];
    float acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        float __anf0 = acc;
        acc = fmaxf(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = fmaxf(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        float res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_17(size_t lena, float *maxs, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = maxs[row];
        float vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_18(size_t lena, float *a_, float *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float acc = 0.0f;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        float v_ = expf(v);
        (void) (acc);
        float __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + 1024);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < 1024) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < 1024) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        float res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_19(size_t lena, float *sums, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = sums[row];
        float vb = a_[col];
        a_[col] = (expf(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_20(double *a_, size_t nthm, size_t lena,
                                        double *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double v0 = a_[tid];
    double acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        double __anf0 = acc;
        acc = fmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = fmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        double res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_21(size_t lena, double *maxs, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = maxs[row];
        double vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_22(size_t lena, double *a_, double *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double acc = 0.0;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        double v_ = exp(v);
        (void) (acc);
        double __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + 1024);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < 1024) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < 1024) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        double res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_23(size_t lena, double *sums, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = sums[row];
        double vb = a_[col];
        a_[col] = (exp(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_24(custard_f16 *a_, size_t nthm,
                                        size_t lena, custard_f16 *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 v0 = a_[tid];
    custard_f16 acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        custard_f16 __anf0 = acc;
        acc = kpr_hfmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = kpr_hfmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        custard_f16 res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_25(size_t lena, custard_f16 *maxs,
                                        custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = maxs[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_sub(vb, va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_26(size_t lena, custard_f16 *a_,
                                        size_t nth, custard_f16 *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 acc = CUSTARD_F16_LIT(0U);
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        custard_f16 v_ = hexp(v);
        (void) (acc);
        custard_f16 __anf0 = acc;
        acc = custard_f16_add(__anf0, v_);
        size_t __anf01 = idx;
        idx = (__anf01 + nth);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nth) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nth) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = custard_f16_add(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        custard_f16 res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_27(size_t lena, custard_f16 *sums,
                                        custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = sums[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_div(hexp(vb), va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_28(float *a_, size_t nthm, size_t lena,
                                        float *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float v0 = a_[tid];
    float acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        float __anf0 = acc;
        acc = fmaxf(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = fmaxf(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        float res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_29(size_t lena, float *maxs, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = maxs[row];
        float vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_30(size_t lena, float *a_, size_t nth,
                                        float *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float acc = 0.0f;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        float v_ = expf(v);
        (void) (acc);
        float __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + nth);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nth) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nth) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        float res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_31(size_t lena, float *sums, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = sums[row];
        float vb = a_[col];
        a_[col] = (expf(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_32(double *a_, size_t nthm, size_t lena,
                                        double *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double v0 = a_[tid];
    double acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        double __anf0 = acc;
        acc = fmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = fmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        double res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_33(size_t lena, double *maxs, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = maxs[row];
        double vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_34(size_t lena, double *a_, size_t nth,
                                        double *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double acc = 0.0;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        double v_ = exp(v);
        (void) (acc);
        double __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + nth);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nth) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nth) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        double res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_35(size_t lena, double *sums, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = sums[row];
        double vb = a_[col];
        a_[col] = (exp(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_36(custard_f16 *a_, size_t nthm,
                                        size_t lena, custard_f16 *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 v0 = a_[tid];
    custard_f16 acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        custard_f16 __anf0 = acc;
        acc = kpr_hfmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = kpr_hfmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        custard_f16 res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_37(size_t lena, custard_f16 *maxs,
                                        custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = maxs[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_sub(vb, va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_38(size_t lena, custard_f16 *a_,
                                        custard_f16 *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    custard_f16 *tmp1 = (custard_f16 *) tmp;
    FStar_Pervasives_Native_tuple2__float16_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float16_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    custard_f16 acc = CUSTARD_F16_LIT(0U);
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        custard_f16 v = a_[idx_raw];
        custard_f16 v_ = hexp(v);
        (void) (acc);
        custard_f16 __anf0 = acc;
        acc = custard_f16_add(__anf0, v_);
        size_t __anf01 = idx;
        idx = (__anf01 + 1024);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < 1024) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < 1024) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                custard_f16 s1 = shmem._1[tid];
                custard_f16 s2 = shmem._1[nextid];
                custard_f16 s = custard_f16_add(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        custard_f16 res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_39(size_t lena, custard_f16 *sums,
                                        custard_f16 *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        custard_f16 va = sums[row];
        custard_f16 vb = a_[col];
        a_[col] = custard_f16_div(hexp(vb), va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_40(float *a_, size_t nthm, size_t lena,
                                        float *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float v0 = a_[tid];
    float acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        float __anf0 = acc;
        acc = fmaxf(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = fmaxf(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        float res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_41(size_t lena, float *maxs, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = maxs[row];
        float vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_42(size_t lena, float *a_, float *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    float *tmp1 = (float *) tmp;
    FStar_Pervasives_Native_tuple2__float32_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float32_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    float acc = 0.0f;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        float v = a_[idx_raw];
        float v_ = expf(v);
        (void) (acc);
        float __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + 1024);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < 1024) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < 1024) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                float s1 = shmem._1[tid];
                float s2 = shmem._1[nextid];
                float s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        float res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_43(size_t lena, float *sums, float *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        float va = sums[row];
        float vb = a_[col];
        a_[col] = (expf(vb) / va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_44(double *a_, size_t nthm, size_t lena,
                                        double *maxs)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double v0 = a_[tid];
    double acc = v0;
    size_t idx = (tid + nthm);
    while (idx < lena) {
        (void) (idx);
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        (void) (idx);
        (void) (idx);
        (void) (idx);
        double __anf0 = acc;
        acc = fmax(__anf0, v);
        size_t __anf01 = idx;
        idx = (__anf01 + nthm);
    }
    (void) (idx);
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < nthm) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < nthm) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = fmax(s1, s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni2 = 0;
        double res = shmem._1[ni2];
        maxs[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_45(size_t lena, double *maxs, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = maxs[row];
        double vb = a_[col];
        a_[col] = (vb - va);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_46(size_t lena, double *a_, double *sums)
{
    uint8_t *tmp = (uint8_t *) KPR_SHMEM_AT(((size_t) 0ULL));
    double *tmp1 = (double *) tmp;
    FStar_Pervasives_Native_tuple2__float64_ptr_unit shmem =
        (FStar_Pervasives_Native_tuple2__float64_ptr_unit) {._1 = tmp1};
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    double acc = 0.0;
    size_t idx = tid;
    while (idx < lena) {
        size_t idx_raw = idx;
        double v = a_[idx_raw];
        double v_ = exp(v);
        (void) (acc);
        double __anf0 = acc;
        acc = (__anf0 + v_);
        size_t __anf01 = idx;
        idx = (__anf01 + 1024);
    }
    shmem._1[tid] = acc;
    size_t n = 0;
    (void) (n);
    (void) (n);
    (void) (n);
    while ((size_t) (((uint32_t) 1U) << (uint32_t) n) < 1024) {
        size_t __anf0_1 = n;
        __syncthreads();
        size_t nextid =
            (tid + (size_t) (((uint32_t) 1U) << (uint32_t) __anf0_1));
        if (nextid < 1024) {
            bool b = ((tid & ((size_t) (((uint32_t) 1U)
                                        << (uint32_t) (__anf0_1 + 1)) -
                              1)) == 0);
            if (b) {
                double s1 = shmem._1[tid];
                double s2 = shmem._1[nextid];
                double s = (s1 + s2);
                shmem._1[tid] = s;
            }
        }
        size_t __anf01_1 = n;
        n = (__anf01_1 + 1);
    }
    if (tid == 0) {
        size_t ni1 = 0;
        double res = shmem._1[ni1];
        sums[bid] = res;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_47(size_t lena, double *sums, double *a_)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * lena)) {
        size_t row = (gid / lena);
        size_t col = (gid % lena);
        double va = sums[row];
        double vb = a_[col];
        a_[col] = (exp(vb) / va);
    }
}

void Klas_Softmax_softmax_gpu_n_f16(size_t nth, size_t lena, custard_f16 *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *maxs = (custard_f16 *) tmp;
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *sums = (custard_f16 *) tmp1;
    size_t nthm;
    if (nth <= lena)
        nthm = nth;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (2 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_0, (0 + (2 * nthm)));
    KPR_KCALL(kuiper_kernel_0, ((size_t) 1ULL), nthm, (0 + (2 * nthm)), s, a,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_1, (((1 * lena) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s1, lena, maxs, a);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (2 * nth)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_2, (0 + (2 * nth)));
    KPR_KCALL(kuiper_kernel_2, ((size_t) 1ULL), nth, (0 + (2 * nth)), s2, lena,
              a, nth, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_3, (((1 * lena) / 1024) + _ct2), ((size_t) 1024ULL),
              ((size_t) 0ULL), s3, lena, sums, a);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
}

void Klas_Softmax_softmax_gpu_n_f32(size_t nth, size_t lena, float *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *maxs = (float *) tmp;
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *sums = (float *) tmp1;
    size_t nthm;
    if (nth <= lena)
        nthm = nth;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (4 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_4, (0 + (4 * nthm)));
    KPR_KCALL(kuiper_kernel_4, ((size_t) 1ULL), nthm, (0 + (4 * nthm)), s, a,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_5, (((1 * lena) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s1, lena, maxs, a);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (4 * nth)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_6, (0 + (4 * nth)));
    KPR_KCALL(kuiper_kernel_6, ((size_t) 1ULL), nth, (0 + (4 * nth)), s2, lena,
              a, nth, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_7, (((1 * lena) / 1024) + _ct2), ((size_t) 1024ULL),
              ((size_t) 0ULL), s3, lena, sums, a);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
}

void Klas_Softmax_softmax_gpu_n_f64(size_t nth, size_t lena, double *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *maxs = (double *) tmp;
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *sums = (double *) tmp1;
    size_t nthm;
    if (nth <= lena)
        nthm = nth;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (8 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_8, (0 + (8 * nthm)));
    KPR_KCALL(kuiper_kernel_8, ((size_t) 1ULL), nthm, (0 + (8 * nthm)), s, a,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_9, (((1 * lena) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s1, lena, maxs, a);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (8 * nth)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_10, (0 + (8 * nth)));
    KPR_KCALL(kuiper_kernel_10, ((size_t) 1ULL), nth, (0 + (8 * nth)), s2, lena,
              a, nth, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_11, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, a);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
}

void Klas_Softmax_softmax_gpu_f16(size_t lena, custard_f16 *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *maxs = (custard_f16 *) tmp;
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *sums = (custard_f16 *) tmp1;
    size_t nthm;
    if (1024 <= lena)
        nthm = 1024;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (2 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_12, (0 + (2 * nthm)));
    KPR_KCALL(kuiper_kernel_12, ((size_t) 1ULL), nthm, (0 + (2 * nthm)), s, a,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_13, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, a);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (((size_t) 2ULL) * ((size_t) 1024ULL))));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_14,
                          (0 + (((size_t) 2ULL) * ((size_t) 1024ULL))));
    KPR_KCALL(kuiper_kernel_14, ((size_t) 1ULL), ((size_t) 1024ULL),
              (0 + (((size_t) 2ULL) * ((size_t) 1024ULL))), s2, lena, a, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_15, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, a);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
}

void Klas_Softmax_softmax_gpu_f32(size_t lena, float *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *maxs = (float *) tmp;
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *sums = (float *) tmp1;
    size_t nthm;
    if (1024 <= lena)
        nthm = 1024;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (4 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_16, (0 + (4 * nthm)));
    KPR_KCALL(kuiper_kernel_16, ((size_t) 1ULL), nthm, (0 + (4 * nthm)), s, a,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_17, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, a);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (((size_t) 4ULL) * ((size_t) 1024ULL))));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_18,
                          (0 + (((size_t) 4ULL) * ((size_t) 1024ULL))));
    KPR_KCALL(kuiper_kernel_18, ((size_t) 1ULL), ((size_t) 1024ULL),
              (0 + (((size_t) 4ULL) * ((size_t) 1024ULL))), s2, lena, a, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_19, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, a);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
}

void Klas_Softmax_softmax_gpu_f64(size_t lena, double *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *maxs = (double *) tmp;
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *sums = (double *) tmp1;
    size_t nthm;
    if (1024 <= lena)
        nthm = 1024;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (8 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_20, (0 + (8 * nthm)));
    KPR_KCALL(kuiper_kernel_20, ((size_t) 1ULL), nthm, (0 + (8 * nthm)), s, a,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_21, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, a);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (((size_t) 8ULL) * ((size_t) 1024ULL))));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_22,
                          (0 + (((size_t) 8ULL) * ((size_t) 1024ULL))));
    KPR_KCALL(kuiper_kernel_22, ((size_t) 1ULL), ((size_t) 1024ULL),
              (0 + (((size_t) 8ULL) * ((size_t) 1024ULL))), s2, lena, a, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_23, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, a);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
}

void Klas_Softmax_softmax_n_f16(size_t nth, size_t lena, custard_f16 *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), lena);
    custard_f16 *ga = (custard_f16 *) tmp;
    KPR_MEMCPY_H2D(ga, a, (2 * lena));
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *maxs = (custard_f16 *) tmp1;
    uint8_t *tmp2 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *sums = (custard_f16 *) tmp2;
    size_t nthm;
    if (nth <= lena)
        nthm = nth;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (2 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_24, (0 + (2 * nthm)));
    KPR_KCALL(kuiper_kernel_24, ((size_t) 1ULL), nthm, (0 + (2 * nthm)), s, ga,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_25, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, ga);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (2 * nth)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_26, (0 + (2 * nth)));
    KPR_KCALL(kuiper_kernel_26, ((size_t) 1ULL), nth, (0 + (2 * nth)), s2, lena,
              ga, nth, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_27, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, ga);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
    KPR_MEMCPY_D2H(a, ga, (2 * lena));
    KPR_GPU_FREE(ga);
}

void Klas_Softmax_softmax_n_f32(size_t nth, size_t lena, float *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), lena);
    float *ga = (float *) tmp;
    KPR_MEMCPY_H2D(ga, a, (4 * lena));
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *maxs = (float *) tmp1;
    uint8_t *tmp2 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *sums = (float *) tmp2;
    size_t nthm;
    if (nth <= lena)
        nthm = nth;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (4 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_28, (0 + (4 * nthm)));
    KPR_KCALL(kuiper_kernel_28, ((size_t) 1ULL), nthm, (0 + (4 * nthm)), s, ga,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_29, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, ga);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (4 * nth)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_30, (0 + (4 * nth)));
    KPR_KCALL(kuiper_kernel_30, ((size_t) 1ULL), nth, (0 + (4 * nth)), s2, lena,
              ga, nth, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_31, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, ga);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
    KPR_MEMCPY_D2H(a, ga, (4 * lena));
    KPR_GPU_FREE(ga);
}

void Klas_Softmax_softmax_n_f64(size_t nth, size_t lena, double *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), lena);
    double *ga = (double *) tmp;
    KPR_MEMCPY_H2D(ga, a, (8 * lena));
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *maxs = (double *) tmp1;
    uint8_t *tmp2 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *sums = (double *) tmp2;
    size_t nthm;
    if (nth <= lena)
        nthm = nth;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (8 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_32, (0 + (8 * nthm)));
    KPR_KCALL(kuiper_kernel_32, ((size_t) 1ULL), nthm, (0 + (8 * nthm)), s, ga,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_33, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, ga);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (8 * nth)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_34, (0 + (8 * nth)));
    KPR_KCALL(kuiper_kernel_34, ((size_t) 1ULL), nth, (0 + (8 * nth)), s2, lena,
              ga, nth, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_35, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, ga);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
    KPR_MEMCPY_D2H(a, ga, (8 * lena));
    KPR_GPU_FREE(ga);
}

void Klas_Softmax_softmax_f16(size_t lena, custard_f16 *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), lena);
    custard_f16 *ga = (custard_f16 *) tmp;
    KPR_MEMCPY_H2D(ga, a, (2 * lena));
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *maxs = (custard_f16 *) tmp1;
    uint8_t *tmp2 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 2ULL), ((size_t) 1ULL));
    custard_f16 *sums = (custard_f16 *) tmp2;
    size_t nthm;
    if (1024 <= lena)
        nthm = 1024;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (2 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_36, (0 + (2 * nthm)));
    KPR_KCALL(kuiper_kernel_36, ((size_t) 1ULL), nthm, (0 + (2 * nthm)), s, ga,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_37, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, ga);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (((size_t) 2ULL) * ((size_t) 1024ULL))));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_38,
                          (0 + (((size_t) 2ULL) * ((size_t) 1024ULL))));
    KPR_KCALL(kuiper_kernel_38, ((size_t) 1ULL), ((size_t) 1024ULL),
              (0 + (((size_t) 2ULL) * ((size_t) 1024ULL))), s2, lena, ga, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_39, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, ga);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
    KPR_MEMCPY_D2H(a, ga, (2 * lena));
    KPR_GPU_FREE(ga);
}

void Klas_Softmax_softmax_f32(size_t lena, float *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), lena);
    float *ga = (float *) tmp;
    KPR_MEMCPY_H2D(ga, a, (4 * lena));
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *maxs = (float *) tmp1;
    uint8_t *tmp2 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 4ULL), ((size_t) 1ULL));
    float *sums = (float *) tmp2;
    size_t nthm;
    if (1024 <= lena)
        nthm = 1024;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (4 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_40, (0 + (4 * nthm)));
    KPR_KCALL(kuiper_kernel_40, ((size_t) 1ULL), nthm, (0 + (4 * nthm)), s, ga,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_41, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, ga);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (((size_t) 4ULL) * ((size_t) 1024ULL))));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_42,
                          (0 + (((size_t) 4ULL) * ((size_t) 1024ULL))));
    KPR_KCALL(kuiper_kernel_42, ((size_t) 1ULL), ((size_t) 1024ULL),
              (0 + (((size_t) 4ULL) * ((size_t) 1024ULL))), s2, lena, ga, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_43, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, ga);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
    KPR_MEMCPY_D2H(a, ga, (4 * lena));
    KPR_GPU_FREE(ga);
}

void Klas_Softmax_softmax_f64(size_t lena, double *a)
{
    uint8_t *tmp = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), lena);
    double *ga = (double *) tmp;
    KPR_MEMCPY_H2D(ga, a, (8 * lena));
    uint8_t *tmp1 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *maxs = (double *) tmp1;
    uint8_t *tmp2 = (uint8_t *) KPR_GPU_ALLOC(((size_t) 8ULL), ((size_t) 1ULL));
    double *sums = (double *) tmp2;
    size_t nthm;
    if (1024 <= lena)
        nthm = 1024;
    else
        nthm = lena;
    cudaStream_t s = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (8 * nthm)));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_44, (0 + (8 * nthm)));
    KPR_KCALL(kuiper_kernel_44, ((size_t) 1ULL), nthm, (0 + (8 * nthm)), s, ga,
              nthm, lena, maxs);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
    cudaStream_t s1 = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * lena) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_45, (((1 * lena) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s1, lena, maxs, ga);
    KPR_MUST_stream_sync(s1);
    KPR_MUST_stream_destroy(s1);
    cudaStream_t s2 = KPR_FRESH_STREAM();
    KPR_SHMEM_FITS((0 + (((size_t) 8ULL) * ((size_t) 1024ULL))));
    KPR_SET_MAX_DYN_SHMEM(kuiper_kernel_46,
                          (0 + (((size_t) 8ULL) * ((size_t) 1024ULL))));
    KPR_KCALL(kuiper_kernel_46, ((size_t) 1ULL), ((size_t) 1024ULL),
              (0 + (((size_t) 8ULL) * ((size_t) 1024ULL))), s2, lena, ga, sums);
    KPR_MUST_stream_sync(s2);
    KPR_MUST_stream_destroy(s2);
    cudaStream_t s3 = KPR_FRESH_STREAM();
    size_t _ct2;
    if (((1 * lena) % 1024) != 0)
        _ct2 = 1;
    else
        _ct2 = 0;
    KPR_KCALL(kuiper_kernel_47, (((1 * lena) / 1024) + _ct2),
              ((size_t) 1024ULL), ((size_t) 0ULL), s3, lena, sums, ga);
    KPR_MUST_stream_sync(s3);
    KPR_MUST_stream_destroy(s3);
    KPR_GPU_FREE(sums);
    KPR_GPU_FREE(maxs);
    KPR_MEMCPY_D2H(a, ga, (8 * lena));
    KPR_GPU_FREE(ga);
}
