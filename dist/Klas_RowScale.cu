/* Generated by F* Custard extraction. Do not edit. */
#include "Klas_RowScale.h"

__global__ static void kuiper_kernel_0(
    uint32_t m, uint32_t n, custard_f16 *a, custard_f16 *b);
__global__ static void kuiper_kernel_1(
    uint32_t m, uint32_t n, custard_f16 *a, custard_f16 *b);
__global__ static void kuiper_kernel_2(
    uint32_t m, uint32_t n, float *a, float *b);
__global__ static void kuiper_kernel_3(
    uint32_t m, uint32_t n, float *a, float *b);
__global__ static void kuiper_kernel_4(
    uint32_t m, uint32_t n, double *a, double *b);
__global__ static void kuiper_kernel_5(
    uint32_t m, uint32_t n, double *a, double *b);

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(
    uint32_t m, uint32_t n, custard_f16 *a, custard_f16 *b)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        uint32_t row = (gid / n);
        uint32_t col = (gid % n);
        custard_f16 va = a[row];
        uint32_t ni1 = ((row * n) + col);
        custard_f16 vb = b[ni1];
        uint32_t ni2 = ((row * n) + col);
        b[ni2] = custard_f16_mul(va, vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_1(
    uint32_t m, uint32_t n, custard_f16 *a, custard_f16 *b)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        uint32_t row = (gid / n);
        uint32_t col = (gid % n);
        custard_f16 va = a[row];
        uint32_t ni1 = ((col * m) + row);
        custard_f16 vb = b[ni1];
        uint32_t ni2 = ((col * m) + row);
        b[ni2] = custard_f16_mul(va, vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_2(
    uint32_t m, uint32_t n, float *a, float *b)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        uint32_t row = (gid / n);
        uint32_t col = (gid % n);
        float va = a[row];
        uint32_t ni1 = ((row * n) + col);
        float vb = b[ni1];
        uint32_t ni2 = ((row * n) + col);
        b[ni2] = (va * vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_3(
    uint32_t m, uint32_t n, float *a, float *b)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        uint32_t row = (gid / n);
        uint32_t col = (gid % n);
        float va = a[row];
        uint32_t ni1 = ((col * m) + row);
        float vb = b[ni1];
        uint32_t ni2 = ((col * m) + row);
        b[ni2] = (va * vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_4(
    uint32_t m, uint32_t n, double *a, double *b)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        uint32_t row = (gid / n);
        uint32_t col = (gid % n);
        double va = a[row];
        uint32_t ni1 = ((row * n) + col);
        double vb = b[ni1];
        uint32_t ni2 = ((row * n) + col);
        b[ni2] = (va * vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_5(
    uint32_t m, uint32_t n, double *a, double *b)
{
    uint32_t bid = blockIdx.x;
    uint32_t tid = threadIdx.x;
    uint32_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        uint32_t row = (gid / n);
        uint32_t col = (gid % n);
        double va = a[row];
        uint32_t ni1 = ((col * m) + row);
        double vb = b[ni1];
        uint32_t ni2 = ((col * m) + row);
        b[ni2] = (va * vb);
    }
}

void Klas_RowScale_rowscale_f16_rowmajor(
    uint32_t m, uint32_t n, custard_f16 *a, custard_f16 *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_0, (((m * n) / 1024) + _ct1), ((uint32_t) 1024U),
        ((uint32_t) 0U), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f16_colmajor(
    uint32_t m, uint32_t n, custard_f16 *a, custard_f16 *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_1, (((m * n) / 1024) + _ct1), ((uint32_t) 1024U),
        ((uint32_t) 0U), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f32_rowmajor(
    uint32_t m, uint32_t n, float *a, float *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_2, (((m * n) / 1024) + _ct1), ((uint32_t) 1024U),
        ((uint32_t) 0U), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f32_colmajor(
    uint32_t m, uint32_t n, float *a, float *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_3, (((m * n) / 1024) + _ct1), ((uint32_t) 1024U),
        ((uint32_t) 0U), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f64_rowmajor(
    uint32_t m, uint32_t n, double *a, double *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_4, (((m * n) / 1024) + _ct1), ((uint32_t) 1024U),
        ((uint32_t) 0U), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f64_colmajor(
    uint32_t m, uint32_t n, double *a, double *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    uint32_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_5, (((m * n) / 1024) + _ct1), ((uint32_t) 1024U),
        ((uint32_t) 0U), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}
