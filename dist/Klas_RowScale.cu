/* Generated by F* Custard extraction. Do not edit. */
#include "Klas_RowScale.h"

__global__ static void kuiper_kernel_0(size_t m, size_t n, custard_f16 *a,
                                       custard_f16 *b);
__global__ static void kuiper_kernel_1(size_t m, size_t n, custard_f16 *a,
                                       custard_f16 *b);
__global__ static void kuiper_kernel_2(size_t m, size_t n, float *a, float *b);
__global__ static void kuiper_kernel_3(size_t m, size_t n, float *a, float *b);
__global__ static void kuiper_kernel_4(size_t m, size_t n, double *a,
                                       double *b);
__global__ static void kuiper_kernel_5(size_t m, size_t n, double *a,
                                       double *b);

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(size_t m, size_t n, custard_f16 *a,
                                       custard_f16 *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        size_t row = (gid / n);
        size_t col = (gid % n);
        custard_f16 va = a[row];
        size_t ni1 = ((row * n) + col);
        custard_f16 vb = b[ni1];
        size_t ni2 = ((row * n) + col);
        b[ni2] = custard_f16_mul(va, vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_1(size_t m, size_t n, custard_f16 *a,
                                       custard_f16 *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        size_t row = (gid / n);
        size_t col = (gid % n);
        custard_f16 va = a[row];
        size_t ni1 = ((col * m) + row);
        custard_f16 vb = b[ni1];
        size_t ni2 = ((col * m) + row);
        b[ni2] = custard_f16_mul(va, vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_2(size_t m, size_t n, float *a, float *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        size_t row = (gid / n);
        size_t col = (gid % n);
        float va = a[row];
        size_t ni1 = ((row * n) + col);
        float vb = b[ni1];
        size_t ni2 = ((row * n) + col);
        b[ni2] = (va * vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_3(size_t m, size_t n, float *a, float *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        size_t row = (gid / n);
        size_t col = (gid % n);
        float va = a[row];
        size_t ni1 = ((col * m) + row);
        float vb = b[ni1];
        size_t ni2 = ((col * m) + row);
        b[ni2] = (va * vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_4(size_t m, size_t n, double *a, double *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        size_t row = (gid / n);
        size_t col = (gid % n);
        double va = a[row];
        size_t ni1 = ((row * n) + col);
        double vb = b[ni1];
        size_t ni2 = ((row * n) + col);
        b[ni2] = (va * vb);
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_5(size_t m, size_t n, double *a, double *b)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (m * n)) {
        size_t row = (gid / n);
        size_t col = (gid % n);
        double va = a[row];
        size_t ni1 = ((col * m) + row);
        double vb = b[ni1];
        size_t ni2 = ((col * m) + row);
        b[ni2] = (va * vb);
    }
}

void Klas_RowScale_rowscale_f16_rowmajor(size_t m, size_t n, custard_f16 *a,
                                         custard_f16 *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_0, (((m * n) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f16_colmajor(size_t m, size_t n, custard_f16 *a,
                                         custard_f16 *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_1, (((m * n) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f32_rowmajor(size_t m, size_t n, float *a, float *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_2, (((m * n) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f32_colmajor(size_t m, size_t n, float *a, float *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_3, (((m * n) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f64_rowmajor(size_t m, size_t n, double *a,
                                         double *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_4, (((m * n) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_RowScale_rowscale_f64_colmajor(size_t m, size_t n, double *a,
                                         double *b)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((m * n) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_5, (((m * n) / 1024) + _ct1), ((size_t) 1024ULL),
              ((size_t) 0ULL), s, m, n, a, b);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}
