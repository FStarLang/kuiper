/* Generated by F* Custard extraction. Do not edit. */
#include "Klas_GEMM_Naive2.h"

__global__ static void kuiper_kernel_0(size_t m, size_t n, size_t k, float *gA,
                                       float *gB, float *gC);
__global__ static void kuiper_kernel_1(size_t m, size_t n, size_t k, double *gA,
                                       double *gB, double *gC);
__global__ static void kuiper_kernel_2(size_t m, size_t n, size_t k,
                                       uint32_t *gA, uint32_t *gB,
                                       uint32_t *gC);
__global__ static void kuiper_kernel_3(size_t m, size_t n, size_t k,
                                       uint64_t *gA, uint64_t *gB,
                                       uint64_t *gC);
__global__ static void kuiper_kernel_4(size_t m, size_t n, size_t k, float *gA,
                                       float *gB, float *gC);
__global__ static void kuiper_kernel_5(size_t m, size_t n, size_t k, double *gA,
                                       double *gB, double *gC);
__global__ static void kuiper_kernel_6(size_t m, size_t n, size_t k,
                                       uint32_t *gA, uint32_t *gB,
                                       uint32_t *gC);
__global__ static void kuiper_kernel_7(size_t m, size_t n, size_t k,
                                       uint64_t *gA, uint64_t *gB,
                                       uint64_t *gC);
__global__ static void kuiper_kernel_8(size_t batch, size_t m, size_t n,
                                       size_t k, float *a, float *b, float *c);
__global__ static void kuiper_kernel_9(size_t batch, size_t m, size_t n,
                                       size_t k, float *a, float *b, float *c,
                                       float beta, float alpha);

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_0(size_t m, size_t n, size_t k, float *gA,
                                       float *gB, float *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        float sum = 0.0f;
        while (k1 < k) {
            size_t vk = k1;
            float __anf2 = sum;
            size_t ni = ((trow * k) + vk);
            float __anf1 = gA[ni];
            size_t ni1 = ((vk * n) + tcol);
            float __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((trow * n) + tcol);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((trow * n) + tcol);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_1(size_t m, size_t n, size_t k, double *gA,
                                       double *gB, double *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        double sum = 0.0;
        while (k1 < k) {
            size_t vk = k1;
            double __anf2 = sum;
            size_t ni = ((trow * k) + vk);
            double __anf1 = gA[ni];
            size_t ni1 = ((vk * n) + tcol);
            double __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((trow * n) + tcol);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((trow * n) + tcol);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_2(size_t m, size_t n, size_t k,
                                       uint32_t *gA, uint32_t *gB, uint32_t *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        uint32_t sum = 0;
        while (k1 < k) {
            size_t vk = k1;
            uint32_t __anf2 = sum;
            size_t ni = ((trow * k) + vk);
            uint32_t __anf1 = gA[ni];
            size_t ni1 = ((vk * n) + tcol);
            uint32_t __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((trow * n) + tcol);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((trow * n) + tcol);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_3(size_t m, size_t n, size_t k,
                                       uint64_t *gA, uint64_t *gB, uint64_t *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        uint64_t sum = 0;
        while (k1 < k) {
            size_t vk = k1;
            uint64_t __anf2 = sum;
            size_t ni = ((trow * k) + vk);
            uint64_t __anf1 = gA[ni];
            size_t ni1 = ((vk * n) + tcol);
            uint64_t __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((trow * n) + tcol);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((trow * n) + tcol);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_4(size_t m, size_t n, size_t k, float *gA,
                                       float *gB, float *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        float sum = 0.0f;
        while (k1 < k) {
            size_t vk = k1;
            float __anf2 = sum;
            size_t ni = ((vk * m) + trow);
            float __anf1 = gA[ni];
            size_t ni1 = ((tcol * k) + vk);
            float __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((tcol * m) + trow);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((tcol * m) + trow);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_5(size_t m, size_t n, size_t k, double *gA,
                                       double *gB, double *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        double sum = 0.0;
        while (k1 < k) {
            size_t vk = k1;
            double __anf2 = sum;
            size_t ni = ((vk * m) + trow);
            double __anf1 = gA[ni];
            size_t ni1 = ((tcol * k) + vk);
            double __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((tcol * m) + trow);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((tcol * m) + trow);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_6(size_t m, size_t n, size_t k,
                                       uint32_t *gA, uint32_t *gB, uint32_t *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        uint32_t sum = 0;
        while (k1 < k) {
            size_t vk = k1;
            uint32_t __anf2 = sum;
            size_t ni = ((vk * m) + trow);
            uint32_t __anf1 = gA[ni];
            size_t ni1 = ((tcol * k) + vk);
            uint32_t __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((tcol * m) + trow);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((tcol * m) + trow);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_7(size_t m, size_t n, size_t k,
                                       uint64_t *gA, uint64_t *gB, uint64_t *gC)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (1 * (m * n))) {
        size_t rest = (gid / 1);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        uint64_t sum = 0;
        while (k1 < k) {
            size_t vk = k1;
            uint64_t __anf2 = sum;
            size_t ni = ((vk * m) + trow);
            uint64_t __anf1 = gA[ni];
            size_t ni1 = ((tcol * k) + vk);
            uint64_t __anf0 = gB[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((tcol * m) + trow);
        (void) (gC[ni_1]);
        size_t ni1_1 = ((tcol * m) + trow);
        gC[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_8(size_t batch, size_t m, size_t n,
                                       size_t k, float *a, float *b, float *c)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (batch * (m * n))) {
        size_t page = (gid % batch);
        size_t rest = (gid / batch);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        float sum = 0.0f;
        while (k1 < k) {
            size_t vk = k1;
            float __anf2 = sum;
            size_t ni = ((page * (m * k)) + ((trow * k) + vk));
            float __anf1 = a[ni];
            size_t ni1 = ((page * (k * n)) + ((vk * n) + tcol));
            float __anf0 = b[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((page * (m * n)) + ((trow * n) + tcol));
        (void) (c[ni_1]);
        size_t ni1_1 = ((page * (m * n)) + ((trow * n) + tcol));
        c[ni1_1] = sum;
    }
}

/* hoisted by the Custard Kuiper rule */
__global__ static void kuiper_kernel_9(size_t batch, size_t m, size_t n,
                                       size_t k, float *a, float *b, float *c,
                                       float beta, float alpha)
{
    size_t bid = blockIdx.x;
    size_t tid = threadIdx.x;
    size_t gid = ((1024 * bid) + tid);
    if (gid < (batch * (m * n))) {
        size_t page = (gid % batch);
        size_t rest = (gid / batch);
        size_t trow = (rest / n);
        size_t tcol = (rest % n);
        size_t k1 = 0;
        float sum = 0.0f;
        while (k1 < k) {
            size_t vk = k1;
            float __anf2 = sum;
            size_t ni = ((page * (m * k)) + ((trow * k) + vk));
            float __anf1 = a[ni];
            size_t ni1 = ((page * (k * n)) + ((vk * n) + tcol));
            float __anf0 = b[ni1];
            sum = (__anf2 + (__anf1 * __anf0));
            size_t __anf01 = k1;
            k1 = (__anf01 + 1);
        }
        size_t ni_1 = ((page * (m * n)) + ((trow * n) + tcol));
        float v0 = c[ni_1];
        float v1 = ((beta * v0) + (alpha * sum));
        size_t ni1_1 = ((page * (m * n)) + ((trow * n) + tcol));
        c[ni1_1] = v1;
    }
}

void Klas_GEMM_Naive2_g_matmul_f32_rrr(size_t m, size_t n, size_t k, float *gA,
                                       float *gB, float *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_0, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_f64_rrr(size_t m, size_t n, size_t k, double *gA,
                                       double *gB, double *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_1, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_u32_rrr(size_t m, size_t n, size_t k,
                                       uint32_t *gA, uint32_t *gB, uint32_t *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_2, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_u64_rrr(size_t m, size_t n, size_t k,
                                       uint64_t *gA, uint64_t *gB, uint64_t *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_3, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_f32_ccc(size_t m, size_t n, size_t k, float *gA,
                                       float *gB, float *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_4, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_f64_ccc(size_t m, size_t n, size_t k, double *gA,
                                       double *gB, double *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_5, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_u32_ccc(size_t m, size_t n, size_t k,
                                       uint32_t *gA, uint32_t *gB, uint32_t *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_6, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_g_matmul_u64_ccc(size_t m, size_t n, size_t k,
                                       uint64_t *gA, uint64_t *gB, uint64_t *gC)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((1 * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_7, (((1 * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, m, n, k, gA, gB, gC);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_batched_matmul_f32(size_t batch, size_t m, size_t n,
                                         size_t k, float *a, float *b, float *c)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((batch * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_8, (((batch * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, batch, m, n, k, a, b, c);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}

void Klas_GEMM_Naive2_batched_gemm_f32(float alpha, float beta, size_t batch,
                                       size_t m, size_t n, size_t k, float *a,
                                       float *b, float *c)
{
    cudaStream_t s = KPR_FRESH_STREAM();
    size_t _ct1;
    if (((batch * (m * n)) % 1024) != 0)
        _ct1 = 1;
    else
        _ct1 = 0;
    KPR_KCALL(kuiper_kernel_9, (((batch * (m * n)) / 1024) + _ct1),
              ((size_t) 1024ULL), ((size_t) 0ULL), s, batch, m, n, k, a, b, c,
              beta, alpha);
    KPR_MUST_stream_sync(s);
    KPR_MUST_stream_destroy(s);
}
