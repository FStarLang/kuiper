- Restore types for common flavors of matmul?
- Related to above: reduce duplication in instantiations for Tiled/SHMem/BT1D
- Expose instantiations in Kuiper.GEMM.Naive2.fsti, etc?
- Make Kuiper.Concrete work on arbitrary constant nats
- Try to reduce __anf variables in output CUDA
- Try newer Z3's to see if we can get rid of --retry, --z3refresh, etc.
- Make sure shmem and local arrays get proper sendable predicates with solve,
  see the FIXME in Kernel.HReduce.
- Can we still use memset when initializing an array with __float2half_rn(0.0f)?
  How should we do that cleanly?
- Use `smin 1024sz lena` for thread size in HReduce/Softmax/LogSoftmax? Ideally I want
  a single ocurrence of the ternary operator in the output CUDA. See karamel -fternary
